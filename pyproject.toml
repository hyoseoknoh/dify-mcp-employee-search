[project]
name = "test-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "gytjr4926", email = "gytjr4926@naver.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "mcp[cli]>=2.0.0",
    "uvicorn>=0.52.3",
]

[project.scripts]
test-mcp = "test_mcp:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
